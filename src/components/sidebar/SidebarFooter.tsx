import type { ReactElement } from 'react'

import { SidebarList } from '~/components/sidebar/SidebarList'

// TODO: tbd
const SidebarFooter = (): ReactElement => {
  return <SidebarList>{null}</SidebarList>
}

export default SidebarFooter
